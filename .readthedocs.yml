version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    rust: "latest"

sphinx:
  configuration: crates/despina-py/docs/source/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
